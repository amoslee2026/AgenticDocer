[alembic]
script_location = alembic
prepend_sys_path = .
path_separator = os
version_path_separator = os

# 连接串来源（优先级）：显式 -x sqlalchemy.url / Config.set_main_option
#   → 环境变量 MIGRATION_DATABASE_URL（属主角色，§4.3）
#   → 环境变量 DATABASE_URL
#   → agenticdocer.store.db.DEFAULT_MIGRATION_DATABASE_URL
# 迁移必须以**属主**角色执行（建表 + GRANT/REVOKE）；应用角色无 DDL 权限（A15）。
sqlalchemy.url =

[post_write_hooks]
